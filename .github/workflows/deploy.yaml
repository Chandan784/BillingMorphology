name: CI/CD Frontend - BillingMorphologyAcademy

on:
  push:
    branches:
      - main # Trigger when code is pushed to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Deploy Frontend to VPS
        run: |
          ssh -o StrictHostKeyChecking=no root@82.29.161.242 << 'EOF'
            echo "🔄 Pulling latest frontend code..."
            cd ~/BillingMorphologyAcademy
            git pull origin main

            echo "🧹 Stopping and removing old container..."
            docker stop billing-frontend || true
            docker rm billing-frontend || true

            echo "🧽 Removing old Docker image..."
            docker rmi billing-morphology-academy:latest || true

            echo "🏗️ Building new Docker image..."
            docker build -t billing-morphology-academy:latest .

            echo "🚀 Starting new container..."
            docker run -d \
              --name billing-frontend \
              -p 3000:3000 \
              billing-morphology-academy:latest

            echo "✅ Deployment complete! Frontend running on port 3000."
          EOF
