FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all source files
COPY . .

# Expose port 3000
EXPOSE 3000

# Build and start Next.js app when container runs
CMD npm run build && npm start
